import { PAGE_GUTTER_X } from '@/theme/pageContainer'

/** Shared shell dimensions for app and marketing headers. */
export const HEADER_MIN_HEIGHT = { base: '56px', md: '64px' } as const

/** Horizontal gutters — matches the shared page Container. */
export const HEADER_PADDING_X = PAGE_GUTTER_X
