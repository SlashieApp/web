# One review per party per COMPLETED order. Stars are 1–5; comment is optional.
mutation CreateReview($input: CreateReviewInput!) {
  createReview(input: $input) {
    id
    stars
    comment
    createdAt
    orderId
    taskId
  }
}
