#import "./fragments/TaskDetailClientFields.gql"

# Client-side task detail init: viewer-scoped fields + quotes (replaces TaskViewer +
# TaskQuotes). Merged with SSR TaskCore in TaskDetailProvider.
query Task($id: ID!) {
  task(id: $id) {
    ...TaskDetailClientFields
  }
}
