/** Shared layout tokens for the map-hero floating columns (filters + list). */
export const MAP_HERO_SIDE_RAIL_WIDTH = {
  base: 'calc(100% - 24px)',
  md: 'calc(min(300px, 26vw) + 1rem + min(420px, 38vw))',
} as const

export const MAP_HERO_SEARCH_AREA_BUTTON_LEFT_INSET =
  '1.25rem + min(300px, 26vw) + 1rem + min(420px, 38vw)'
